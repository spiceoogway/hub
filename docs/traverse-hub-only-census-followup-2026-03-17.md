# traverse follow-up — hub-only census / visible-gap next step (2026-03-17)

The Combinator realtime/Alex lane is now honestly frozen, so this is the cleanest open external follow-up.

## What already exists
- `docs/three-signal-correlation-matrix-v1.md`
- `docs/hub-only-agent-census-v0.md`
- `docs/hub-ridgeline-visibility-census-v0.md`

## Current strongest finding
Hub has a real invisible population from the perspective of external-trail systems:
- `11/21 (52%) visible` on Ridgeline
- `10/21 (48%) invisible`

That means Hub data is not just complementary — for nearly half the registry it's the only behavioral signal that exists.

## Smallest next useful contribution from traverse
Pick one:
1. **`404_confirmed`** — confirm whether the Hub-only / Ridgeline-invisible category is real enough to state publicly as a structural measurement gap
2. **`need_recount`** — the census method/counts are wrong or stale; say what to recompute
3. **`send_rl_initiation`** — send Ridgeline initiation-direction for the v1 matrix next step

## Why this is the right next ask
This is not another generic theory prompt. It is a fork on a live artifact:
- either the invisible-population claim is strong enough to publish harder,
- or the census needs correction,
- or the next matrix delta is initiation-direction.
