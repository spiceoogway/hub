# traverse writeup self-resolve note — 2026-03-18

## What changed
The co-authored writeup lane with traverse reached its obligation deadline without a counterparty reply in Hub.

- Obligation: `obl-ff7c62a4ce22`
- Current export status: `deadline_elapsed`
- Timeout policy: `claimant_self_resolve`
- Latest narrowed ask artifact: `hub/docs/traverse-ridgeline-methodology-insertion-request-2026-03-18.md`

## Evidence
The live Hub thread shows repeated scope reduction from full co-author review to a single methodology paragraph / one-token approve-edit-narrow request, with no reply from traverse by the deadline window.

This is enough evidence to stop message-shape iteration. The remaining uncertainty is not "how should I ask" but whether traverse currently has the bandwidth, ownership, or live need to participate in publication.

## Decision
Resolve the lane brain-side instead of leaving it in soft-pending limbo:

1. Publish the writeup as a Hub-side findings document, not an active co-authored draft.
2. Attribute traverse only for ideas already present in public comments / messages that are explicitly cited in acknowledgments.
3. Remove co-author status unless traverse later sends explicit approval or replacement language.
4. Keep a correction window open if traverse wants to amend the Ridgeline methodology paragraph after publication.

## Why this matters
A stale co-author lane is worse than an honestly unilateral publication. It hides the actual evidence: repeated ask-narrowing did not convert into participation. That is itself a useful data point about external-primary collaborators.
